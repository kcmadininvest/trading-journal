/**
 * Référence des espacements (le rendu effectif est dans PageShell + .layout-page-shell-y dans index.css).
 */
export const PAGE_PADDING_X = 'px-3 sm:px-4 md:px-6 lg:px-8';

/** Aligné sur .layout-page-shell-y (1.5rem haut et bas). */
export const PAGE_PADDING_Y = 'py-6';
